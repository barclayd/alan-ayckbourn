# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# scraper HTTP caches — large, regenerable, never committed. `.cache/blog` also
# holds the R2 upload manifest, which is a record of one machine's uploads.
.cache/

# logs
*.log
npm-debug.log*

# macOS
.DS_Store

# editors
.idea/
.vscode/

# lock files from other package managers
package-lock.json
yarn.lock
pnpm-lock.yaml

# wrangler
.wrangler/
.dev.vars
